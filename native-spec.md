# 串口值匹配器规格

本文记录当前产品规格。历史任务计划不作为当前需求来源；如本文与源码、CI/Release artifact 或最新验收结论冲突，以后者为准。

## 产品定位

串口值匹配器面向中文用户，优先解决 Windows 现场串口调试、Modbus RTU 扫描和“已知真实值匹配寄存器”的问题。

当前主发布形态：

- Win32 native x64 便携包；
- 解压即用；
- 不依赖 Qt 运行库；
- 不依赖 SQLite 插件；
- 不依赖 .NET/C# 运行库。

## 用户

- 设备联调人员；
- 串口协议调试人员；
- Modbus RTU 现场排查人员；
- 需要把通信证据导出给同事或客户的人。

界面、错误提示、报告和文档均以中文为主。

## 当前功能范围

串口终端能力：

- 串口枚举、连接、断开、自动重连；
- 完整常用串口参数；
- 文本/HEX/十进制/二进制发送；
- 行尾、编码、发送历史；
- 定时发送、快捷发送、文件发送；
- TX/RX 日志、时间戳、筛选、搜索、复制、导出。

分析能力：

- FC03/FC04 Modbus RTU 只读扫描；
- 进度显示和取消；
- 按当前真实值生成候选；
- 保存并验证规则；
- 导出 Markdown 报告。

存储能力：

- native 文件存储保存 raw I/O、串口配置、发送历史、扫描、候选和验证结果；
- 可见日志使用有界缓存，避免长期运行时 UI 无限增长。

## 安全边界

- Modbus 扫描只使用只读功能码 FC03/FC04；
- 扫描期间冻结会影响串口句柄或参数的控制入口；
- 写串口操作由用户显式触发；
- 候选分析只给出线索，不把单次匹配结果当作最终结论。

## 非目标

当前 v1.0.3 不承诺：

- 完整替代所有商业串口工具；
- 支持所有私有协议自动识别；
- 1000 MB 级可见日志直接塞入编辑框；
- 完全复刻 ATK-XCOM 或 WinForms 的视觉渲染；
- 对没有真实设备验证过的串口芯片行为作硬承诺。

## 后续优先级

1. Windows 真机高负荷串口验收。
2. 更好的串口设备描述和端口识别。
3. 更成熟的候选证据视图。
4. 大日志虚拟化或磁盘背书浏览。
5. 更克制但可读性优先的 UI 排版优化。
